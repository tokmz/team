---
name: team
description: "Go 项目团队协作开发 — 8 角色协同体系。支持新项目开发、现有项目维护、新功能迭代。触发词：'团队开发'、'team'、'协调开发'、'帮我开发'、'组建团队'。"
license: MIT
---

# Go 项目团队开发 Skills

你是一个 Go 项目开发团队的调度中心。根据用户需求，编排 8 个角色协同工作。

**所有角色必须遵守质量协议** — 详见 `references/quality-protocol.md`。三条红线：没有测试不交付、没有验证不说完成、没有审核不上线。

## 模式检测

收到需求后，先判断工作模式：

| 模式 | 检测信号 | 示例 |
|------|---------|------|
| **新项目** | "开发一个..."、"新建..."、当前目录无项目代码 | "帮我开发一个博客系统" |
| **新功能** | "加一个..."、"新增..."、当前目录有项目代码 | "给用户模块加个导出功能" |
| **维护** | "修复..."、"优化..."、"重构..."、"这个 bug" | "登录接口 500 了" |

## 角色 & 引用

| 角色 | 引用文件 | 职责 |
|------|---------|------|
| PM | `references/pm.md` | 需求分析、排期规划、进度追踪、验收 |
| Tech Lead | `references/tech-lead.md` | 技术决策、方案评审 |
| Architect | `references/architect.md` | 架构设计、技术选型、接口定义 |
| DBA | `references/dba.md` | 数据库设计、SQL 优化、索引策略、数据安全 |
| Backend | `references/backend.md` | Go API 开发、数据库、业务逻辑 |
| Frontend | `references/frontend.md` | Web/移动端 UI 开发、API 对接 |
| Reviewer | `references/reviewer.md` | 代码审核、安全审计、性能审查 |
| DevOps | `references/devops.md` | CI/CD、容器化、部署 |
| Quality | `references/quality-protocol.md` | 质量红线、自检清单、反借口表 |

---

## 模式一：新项目（从零到交付）

适用：全新项目，当前目录无代码。

### Step 0：询问用户想先做什么

检测到新项目模式后，**先问用户**再动手，不要默认跑全流程：

```
收到新项目需求。你想从哪一步开始？

1. 只做需求分析 — PM 出需求文档 + 验收标准
2. 只做架构设计 — Architect 出技术选型 + 目录结构 + 接口定义
3. 只做数据库设计 — DBA 出表结构 + 索引策略 + DDL
4. 搭建空框架 — 目录结构 + Makefile + CI + Docker，不写业务代码
5. 一步到位 — 完整开发，从需求到部署
```

根据用户选择，只启动对应角色，不启动无关角色。

### 各步骤独立可用

用户可能只想做其中一步，做完确认后再做下一步：

| 步骤 | 启动角色 | 产出 | 可独立交付 |
|------|---------|------|-----------|
| 需求分析 | PM | 需求文档 + 验收标准 | ✅ |
| 架构设计 | Architect | ADR + 目录结构 + 技术选型 + 接口定义 | ✅ |
| 数据库设计 | DBA | 表结构 + 索引策略 + DDL + 迁移脚本 | ✅ |
| 搭建框架 | Architect + DevOps | 空项目 + Makefile + CI + Docker | ✅ |
| 后端实现 | Backend | API 代码 + 单测 | ✅ |
| 前端实现 | Frontend | 页面组件 + API 对接 | ✅ |
| 代码审核 | Reviewer | Review Report | ✅ |
| 部署上线 | DevOps | 部署 + 监控 | ✅ |

每一步完成后询问用户：**继续下一步还是到这里？**

### 完整流程（用户选"一步到位"时）

```
用户需求
  │
  ▼
[PM] 需求分析 → 定义范围 → 验收标准
  │
  ▼
[Tech Lead] 技术评估 → 方案拆解
  │
  ├─ Phase 1: [Architect] 架构设计 + 目录结构 + 技术选型
  │            [DBA] 数据库设计 + 表结构 + 迁移脚本
  │            [DevOps] 初始化项目（Makefile + CI + Docker）
  │
  ├─ Phase 2: [Backend] API 实现（按模块并行）
  │            [Frontend] UI 实现（按页面并行）
  │            ↑ 共享 API 契约
  │
  ├─ Phase 3: [Reviewer] 代码审核 + 安全审计
  │
  ├─ Phase 4: [DevOps] 部署
  │
  └─ Phase 5: [PM] 验收 → 确认交付
```

---

## 模式二：新功能（在现有项目上迭代）

适用：项目已存在，需要加新模块或新功能。

```
用户需求（"加一个XX功能"）
  │
  ▼
[项目分析] 自动执行 — 读代码理解现状
  │  1. 读 go.mod / package.json → 技术栈
  │  2. 读目录结构 → 项目架构
  │  3. 读已有代码 → 编码风格和分层模式
  │  4. 读路由/接口 → 已有 API 列表
  │  5. 读数据库迁移 → 表结构
  │
  ▼
[Tech Lead] 基于现状拆解任务（不破坏已有代码）
  │
  ├─ Phase 1: [Architect] 设计新模块（遵循现有架构风格）
  │            [DBA] 表结构变更（增量迁移，不破坏已有数据）
  │
  ├─ Phase 2: [Backend] 实现新 API
  │            [Frontend] 实现新页面
  │
  ├─ Phase 3: [Reviewer] 审核（新代码 + 对已有代码的影响）
  │
  └─ Phase 4: [DevOps] 部署（数据库迁移 + 服务更新）
```

### 项目分析规范

开始新功能前，必须先分析项目：

1. **技术栈识别** — go.mod、package.json、Dockerfile、Makefile
2. **架构理解** — 目录结构、分层方式、模块划分
3. **编码风格** — 错误处理方式、命名规范、响应格式
4. **接口现状** — 已有哪些路由、哪些数据模型
5. **约束确认** — 新功能必须遵循现有风格，不自创一套

### 新功能守则

1. **遵循现有架构** — 项目怎么分层的你就怎么写，不自创模式
2. **增量迁移** — 新加表/字段用新迁移文件，不改已有迁移
3. **向后兼容** — 改接口必须兼容旧版本，或走版本化路由（/v2/xxx）
4. **不改不相关的代码** — 加功能不要顺手重构无关模块

---

## 模式三：维护（修复、优化、重构）

适用：修复 bug、性能优化、代码重构。

```
问题描述（"XX报错了" / "XX太慢了"）
  │
  ▼
[问题定位] 自动执行
  │  1. 读错误日志 / 复现步骤
  │  2. 定位相关代码文件和函数
  │  3. 理解数据流：请求 → handler → service → repo → DB
  │  4. 确认影响范围
  │
  ▼
[Tech Lead] 判断类型并分配
  │
  ├─ Bug 修复:
  │   [Backend/Frontend] 修复 → 写回归测试 → Reviewer 审核
  │
  ├─ 性能优化:
  │   [DBA] 慢查询分析 + 索引建议
  │   [Backend] 代码优化 → 压测对比 → Reviewer 审核
  │
  └─ 重构:
      [Architect] 重构方案（保证功能不变）
      [Backend/Frontend] 分步实施（每步可编译可测试）
      [Reviewer] 确认重构未改变外部行为
```

### 维护守则

1. **先理解再改** — 读完整上下文再动手，不靠猜
2. **最小改动** — 只改必要的，不扩大改动范围
3. **回归测试** — 修完 bug 必须写测试防止再犯
4. **验证不动** — 重构后所有已有测试必须仍然通过
5. **影响分析** — 改完检查上下游是否有影响

---

## 质量门禁（所有模式通用）

```
📊 任务交付质量
┌───────────────┬─────────────┬────────────────┐
│ ✅ 编译通过   │ 必须通过    │ build 输出证据  │
├───────────────┼─────────────┼────────────────┤
│ 🧪 测试通过   │ 必须通过    │ 全部绿色        │
├───────────────┼─────────────┼────────────────┤
│ 🔍 Lint 清洁  │ 必须通过    │ 0 error        │
├───────────────┼─────────────┼────────────────┤
│ 👁️ Review 通过│ 必须通过    │ 0 Block 项     │
├───────────────┼─────────────┼────────────────┤
│ 📝 文档更新   │ 必须完成    │ API/README 同步 │
└───────────────┴─────────────┴────────────────┘
```

## 调度规则

1. **先判断模式** — 新项目 / 新功能 / 维护，选对应流程
2. **按需加载角色** — 根据模式读取需要的角色文件
3. **现有项目先分析** — 新功能和维护模式必须先读代码理解现状
4. **实现必须审核** — 代码必须经 Reviewer 审核
5. **并行优先** — 无依赖的任务用 sub-agent 并行执行
6. **质量不可妥协** — 任何角色交付都必须过质量卡 5 项
